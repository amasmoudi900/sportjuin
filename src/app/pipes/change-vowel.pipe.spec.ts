import { ChangeVowelPipe } from './change-vowel.pipe';

describe('ChangeVowelPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVowelPipe();
    expect(pipe).toBeTruthy();
  });
});
